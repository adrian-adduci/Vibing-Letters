# Vibing Letters - Python Dependencies

# Core libraries
numpy>=2.2.0
opencv-python>=4.12.0
pillow>=12.0.0
scipy>=1.16.0

# Asset generation. Only the build pipeline needs these; the codec, the
# extractor and runtime composition do not touch the network.
fal-client>=0.5.0
python-dotenv>=1.0.0

# Retired letterform-morph path. Still imported by src/morphing/ and its tests,
# which the design marks for removal along with the root-level drivers.
perlin-noise>=1.12
easing-functions>=1.0.4
matplotlib>=3.10.0

# Testing
pytest>=7.0.0
pytest-cov>=4.0.0
